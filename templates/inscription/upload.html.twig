{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Inscription avec .csv{% endblock %}

{% block body %}

    {{ form_start(form) }}
    {{ form_rest(form) }}
    <button type="submit" class="btn btn-primary text-uppercase btn-block">Inscrire stagiaires</button>
    {{ form_end(form) }}

{% endblock %}
